<ul class="navbar-nav mr-auto">
    {% for menuItem in menu %}
        <li class="nav-item active">
            <a class="nav-link"
               href="{{ menuItem['route'] }}">{{ menuItem['label'] }} {# <span class="sr-only">(current)</span> #}</a>
        </li>
    {% endfor %}
</ul>